import React from 'react';
import { Typography } from '@mui/material';

interface IProps {}

export function ChatWithClientPage(props: IProps) {
  return (
    <Typography variant='h4' fontWeight='bold'>
      Связь с клиентом
    </Typography>
  );
}
