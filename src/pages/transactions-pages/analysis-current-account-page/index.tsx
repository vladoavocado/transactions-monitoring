import React from 'react';
import { Typography } from '@mui/material';

interface IProps {}

export function AnalysisCurrentAccountPage(props: IProps) {
  return (
    <Typography variant='h4' fontWeight='bold'>
      Анализ текущего счёта клиентов
    </Typography>
  );
}
